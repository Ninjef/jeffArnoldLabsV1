name: site

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - 'pnpm-workspace.yaml'
      - 'package.json'
      - '.github/workflows/site.yml'
  workflow_dispatch:

concurrency:
  group: site-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      AWS_REGION: us-east-1
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v4

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm --filter @jeffarnoldlabs/site check

      - name: Build
        run: pnpm --filter @jeffarnoldlabs/site build

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.SITE_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync to S3
        run: |
          aws s3 sync site/dist/ "s3://${{ secrets.SITE_BUCKET }}/" \
            --delete \
            --exclude "_astro/*" \
            --cache-control "public, max-age=60, must-revalidate"
          aws s3 sync site/dist/_astro/ "s3://${{ secrets.SITE_BUCKET }}/_astro/" \
            --cache-control "public, max-age=31536000, immutable"

      - name: Invalidate CloudFront
        run: |
          INVL_ID=$(aws cloudfront create-invalidation \
            --distribution-id "${{ secrets.CF_DISTRIBUTION_ID }}" \
            --paths "/*" \
            --query 'Invalidation.Id' --output text)
          echo "Invalidation $INVL_ID created; waiting for completion..."
          aws cloudfront wait invalidation-completed \
            --distribution-id "${{ secrets.CF_DISTRIBUTION_ID }}" \
            --id "$INVL_ID"
