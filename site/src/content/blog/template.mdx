---
title: "Template"
description: "Post template for starting point creating new posts"
draft: true
pubDate: 2026-04-15
tags: ["meta"]
---

import { Image } from 'astro:assets';
import usageLimits from './template/usage-limits.png';
import giphy from './template/giphy.gif';
import InteractiveEmbed from '../../components/InteractiveEmbed.astro';

This is a placeholder post. The blog uses Astro content collections with MDX so
interactive viz components can be imported inline.

<Image src={usageLimits} alt="Claude settings panel showing weekly usage limits and additional features" loading="eager" fetchpriority="high" class="rounded border border-slate-200" />

And a GIF (referenced with a plain `<img>` tag — `astro:assets`' `<Image>` component
would re-encode it and drop the animation):

<img src={giphy.src} alt="Animated demo" class="rounded border border-slate-200" />

And an interactive HTML demo, embedded as a scrollable window. Drop the
HTML file into `site/public/blog/<slug>/` and point `src` at it:

<InteractiveEmbed
  src="/blog/template/template-example-embedded-page.html"
  title="Run comparison"
  caption="Drag, hover, click — example interactive demo. Replace with your own HTML file."
/>

```ts
const example = 42;
```
